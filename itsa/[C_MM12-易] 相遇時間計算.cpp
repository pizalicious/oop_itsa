#include <bits/stdc++.h>
using namespace std;

int main() {
    //freopen ( "in.txt", "r", stdin );
    //freopen ( "in.txt", "w", stdout );
    float a, b, c, d, e, i;
    int ans;
    while ( cin >> a ) {
        ans = ( a / 0.238 ) + 1;
        cout  << ans << endl;
        //cout << fixed << setprecision ( 1 ) << ans << endl;
    }
}
