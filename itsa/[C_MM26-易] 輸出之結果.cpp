#include <bits/stdc++.h>
using namespace std;

int main() {
    //freopen ( "in.txt", "r", stdin );
    //freopen ( "in.txt", "w", stdout );
    int a, b, c, d, e, i;
    int ans = 0;
    while ( cin >> a ) {
        for ( i = 1; i <= a; i++ )
            cout << i << "*" << i << "=" << i *i << endl;
        //cout << fixed << setprecision ( 1 ) << ans << endl;
    }
}
